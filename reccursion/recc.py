def fib(n):
    if n <= 1:return (n)
    return fib(n-1) +fib(n-2)   

print(fib(10))
#step 4 from all the sol from all the  sub program combining al the solution together 
#it works on the prici on div and conc